# Archived one-off scripts

These files were used during page builds, audits, and content fixes. They are kept for reference but are not part of the CI or deploy pipeline.

Active verification lives in `tools/verify-*.js` and `tools/monitor-live.js`.
